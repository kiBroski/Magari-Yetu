'use client'
import { FormEvent, useState } from 'react'
const options = ['certified-mechanic', 'auto-electrical', 'body-painting', 'tinting', 'detailing', 'identity-marking', 'inspection', 'spare-parts', 'car-hire', 'leasing', 'towing']
export function ServiceProviderForm() { const [message, setMessage] = useState(''); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = new FormData(e.currentTarget); const data = Object.fromEntries(form); const services = form.getAll('services'); const res = await fetch('/api/service-providers', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...data, services }) }); if (res.status === 401) { location.href = '/login'; return }; setMessage(res.ok ? 'Submitted for verification. Your public service page will appear once approved.' : 'Could not submit your service. Please check the required details.') } return <form onSubmit={submit} className="space-y-4 rounded-lg border border-ink-100 bg-white p-6"><input name="businessName" required placeholder="Business / shop name" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><textarea name="description" required placeholder="Describe what you offer" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><div><p className="mb-2 text-sm font-medium text-ink">Services offered</p><div className="grid grid-cols-2 gap-2 text-sm">{options.map(x => <label key={x}><input type="checkbox" name="services" value={x} className="mr-2" />{x.replace(/-/g, ' ')}</label>)}</div></div><input name="makesServiced" placeholder="Makes / models serviced (optional)" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><div className="grid gap-3 sm:grid-cols-2"><input name="contactPhone" required placeholder="Phone (2547...)" className="rounded border border-ink-100 px-3 py-2 text-sm" /><input name="whatsappNumber" placeholder="WhatsApp number" className="rounded border border-ink-100 px-3 py-2 text-sm" /><input name="county" required placeholder="County" className="rounded border border-ink-100 px-3 py-2 text-sm" /><input name="town" placeholder="Town / area" className="rounded border border-ink-100 px-3 py-2 text-sm" /></div><input name="physicalAddress" placeholder="Physical address (optional)" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><input name="website" type="url" placeholder="Website (optional)" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><button className="rounded bg-stamp px-5 py-3 text-sm font-medium text-white">Submit for verification</button>{message && <p className="text-sm text-ink-400">{message}</p>}</form> }
