'use client'
import { FormEvent, useState } from 'react'
export function ReviewForm({ targetType, targetId }: { targetType: string; targetId: string }) { const [done, setDone] = useState(false); const [error, setError] = useState(''); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = new FormData(e.currentTarget); const response = await fetch('/api/reviews', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ targetType, targetId, rating: Number(form.get('rating')), title: form.get('title'), body: form.get('body') }) }); if (response.status === 401) { location.href = '/login'; return }; if (!response.ok) { setError('Could not submit your review.'); return }; setDone(true) } if (done) return <p className="text-sm text-matatu">Thanks—your review is awaiting moderation.</p>; return <form onSubmit={submit} className="mt-7 space-y-2 rounded border border-ink-100 p-4"><p className="font-medium text-ink">Leave a review</p><select name="rating" required className="rounded border border-ink-100 px-2 py-1 text-sm"><option value="5">5 stars</option><option value="4">4 stars</option><option value="3">3 stars</option><option value="2">2 stars</option><option value="1">1 star</option></select><input name="title" required placeholder="Short title" className="block w-full rounded border border-ink-100 px-3 py-2 text-sm" /><textarea name="body" required placeholder="Describe your experience" className="block w-full rounded border border-ink-100 px-3 py-2 text-sm" /><button className="rounded bg-ink px-3 py-2 text-sm text-white">Submit review</button>{error && <p className="text-xs text-alert">{error}</p>}</form> }
