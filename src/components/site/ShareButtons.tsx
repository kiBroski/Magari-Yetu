'use client'
export function ShareButtons({ title }: { title: string }) { const url = typeof window === 'undefined' ? '' : window.location.href; const text = encodeURIComponent(`${title} on Magariyetu`); return <div className="flex gap-3 text-xs"><a href={`https://wa.me/?text=${text}%20${encodeURIComponent(url)}`} target="_blank" rel="noopener noreferrer" className="text-matatu underline">Share to WhatsApp</a><a href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`} target="_blank" rel="noopener noreferrer" className="text-stamp-dark underline">Share to Facebook</a></div> }
