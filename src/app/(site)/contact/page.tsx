'use client'
import { FormEvent, useState } from 'react'
export default function ContactPage() { const [sent, setSent] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); const r = await fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(f)) }); setSent(r.ok) } return <div className="mx-auto max-w-xl"><h1 className="font-display text-3xl font-bold text-ink">Contact a moderator</h1><p className="mt-2 text-ink-400">Ask for help, appeal moderation, or report a safety concern.</p>{sent ? <p className="mt-6 rounded bg-matatu/10 p-4 text-sm text-matatu">Your message has been sent to the moderation team.</p> : <form onSubmit={submit} className="mt-6 space-y-3 rounded-lg border border-ink-100 bg-white p-6"><input name="name" required placeholder="Your name" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><input name="email" type="email" placeholder="Email (optional)" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><input name="phone" placeholder="Phone (optional)" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><input name="subject" required placeholder="Subject" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><textarea name="message" required placeholder="How can we help?" className="w-full rounded border border-ink-100 px-3 py-2 text-sm" /><button className="rounded bg-ink px-4 py-2 text-sm text-white">Send message</button></form>}</div> }
